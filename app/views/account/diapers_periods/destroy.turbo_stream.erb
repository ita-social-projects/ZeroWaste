<%= turbo_stream.remove(dom_id(@diapers_period)) %>

<%= turbo_stream.replace(
      dom_id(@diapers_periods.last, "delete_button"),
      partial: "account/diapers_periods/partials/index/delete_button",
      locals: { category: @category,
                diapers_period: @diapers_periods.last,
                diapers_periods: @diapers_periods }
    ) unless @diapers_periods.blank? %>

<%= turbo_stream.replace(
      :add_new_link,
      partial: "account/diapers_periods/partials/index/add_new_link",
      locals: { category: @category, unfilled_categories: @unfilled_categories }
    ) %>
